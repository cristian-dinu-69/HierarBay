nvidia-smi 

Check if libcuda.so and libcudart.so are visible:


ls /usr/lib/x86_64-linux-gnu/libcuda*
ls /usr/local/cuda/lib64/libcudart*

sudo apt update
if sudo apt install -y cuda-cudart-12-1 == errors :
    sudo apt install -y nvidia-cuda-toolkit

pip install --upgrade "jax[cuda12]"
pip install arviz pandas matplotlib seaborn tqdm ipywidgets

====================GIT

# ls -al ~/.ssh
Look for files like id_rsa and id_rsa.pub or id_ed25519 and id_ed25519.pub.

If a key already exists: 

✅ You can skip to step 3.

If not: ➡️ Go to step 2 to generate one.


2. Generate a new SSH key

# ssh-keygen -t ed25519 -C "your_email@example.com"
Replace your_email@example.com with your GitHub email.

When prompted for a file name, press Enter to accept default (~/.ssh/id_ed25519)

When prompted for a passphrase, you can press Enter (no passphrase), or enter a secure one.

eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

cat ~/.ssh/id_ed25519.pub
